// static/js/color_conversion.js
document.getElementById('pen_color').addEventListener('input', function() {
    let colorValue = this.value;
    // 如果需要将颜色转换为Tkinter格式，可以在这里处理
    // 例如，将#RRGGBB转换为 Tkinter 识别的颜色名称或格式
});
